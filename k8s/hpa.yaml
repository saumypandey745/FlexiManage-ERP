apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fleximanage-api-hpa
  namespace: fleximanage-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fleximanage-api
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
